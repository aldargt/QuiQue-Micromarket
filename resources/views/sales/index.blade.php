<x-app-layout>
    <x-slot name="header"><h2 class="font-semibold text-xl text-gray-800">Historial de ventas</h2></x-slot>
    <div class="py-8"><div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 space-y-4">
        <div class="flex flex-col gap-3 sm:flex-row sm:justify-between"><form class="flex gap-2"><input name="search" value="{{ $search }}" placeholder="Número de venta" class="rounded-lg border-gray-300"><x-primary-button>Buscar</x-primary-button></form><a href="{{ route('pos.index') }}" class="ui-button-primary">Nueva venta</a></div>
        <div class="overflow-x-auto bg-white shadow-sm sm:rounded-lg"><table class="min-w-full divide-y"><thead class="bg-gray-50"><tr>@foreach (['Venta','Fecha y hora','Responsable','Pago','Total','Estado',''] as $heading)<th class="px-4 py-3 text-left text-xs uppercase text-gray-500">{{ $heading }}</th>@endforeach</tr></thead><tbody class="divide-y">@forelse ($sales as $sale)<tr><td class="px-4 py-3 font-medium">{{ $sale->sale_number }}</td><td class="px-4 py-3 whitespace-nowrap">{{ $sale->confirmed_at->format('d/m/Y H:i') }}</td><td class="px-4 py-3">{{ $sale->user->name }}</td><td class="px-4 py-3">{{ $sale->paymentLabel() }}</td><td class="px-4 py-3">Bs {{ number_format((float) $sale->total, 2) }}</td><td class="px-4 py-3"><span class="inline-flex rounded-full px-2.5 py-1 text-xs font-semibold {{ $sale->status === App\Enums\SaleStatus::Confirmed ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800' }}">{{ $sale->status->label() }}</span></td><td class="px-4 py-3"><a class="text-indigo-600" href="{{ route('sales.show', $sale) }}">Ver detalle</a></td></tr>@empty<tr><td colspan="7" class="p-8 text-center text-gray-500">No existen ventas registradas.</td></tr>@endforelse</tbody></table></div>
        {{ $sales->links() }}
    </div></div>
</x-app-layout>
